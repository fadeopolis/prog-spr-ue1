prog-spr-ue1
============

to build:
	make all

to run:
	./Calc

additional commands in Calc console:
	'.'  ... show contents of stack and input buffer
	':'  ... show contents of stack and input buffer, and clear stack
	'^'  ... clear the console window, ! works only on POSIX conformant terminals !


